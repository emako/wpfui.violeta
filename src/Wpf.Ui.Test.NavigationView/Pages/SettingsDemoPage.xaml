<Page
    x:Class="Wpf.Ui.Test.NavigationView.Pages.SettingsDemoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="26" FontWeight="SemiBold" Text="Settings Page" />

        <Border
            Grid.Row="2"
            Padding="16"
            Background="{DynamicResource ControlFillColorSecondaryBrush}"
            BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="10">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="设置项演示" />
                <TextBlock
                    Margin="0,10,0,0"
                    Text="这里用于验证 IsSettingsVisible、SettingsItem 选中逻辑，以及与 BackButton 可见性的联动。"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
