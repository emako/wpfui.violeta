<Page
    x:Class="Wpf.Ui.Test.NavigationView.Pages.HomeDemoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock FontSize="26" FontWeight="SemiBold" Text="Home Page" />
            <TextBlock
                Margin="0,8,0,0"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="这里是 NavigationView 的默认首页，适合放总览卡片与关键指标。"
                TextWrapping="Wrap" />
        </StackPanel>

        <UniformGrid Grid.Row="2" Columns="2">
            <Border
                Margin="0,0,10,10"
                Padding="14"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="导航状态" />
                    <TextBlock Margin="0,8,0,0" Text="观察 Pane 模式切换、Back 按钮和设置项行为。" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
            <Border
                Margin="10,0,0,10"
                Padding="14"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock FontWeight="SemiBold" Text="交互测试" />
                    <TextBlock Margin="0,8,0,0" Text="点击左侧菜单后，这个 Frame 会切到对应示例 Page。" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</Page>
