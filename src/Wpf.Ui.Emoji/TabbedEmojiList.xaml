<UserControl
    x:Class="Wpf.Ui.Emoji.TabbedEmojiList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:emoji="clr-namespace:Wpf.Ui.Emoji"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:stfu="clr-namespace:Stfu.Wpf"
    x:Name="UserControl_Root"
    d:DesignHeight="66.775"
    d:DesignWidth="108"
    mc:Ignorable="d">
    <FrameworkElement.Resources>

        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <ControlTemplate x:Key="VariationPopupTemplate" TargetType="ContentControl">
            <ListBox
                Name="VariationListView"
                MaxWidth="233"
                Margin="0"
                Padding="0"
                Background="Transparent"
                BorderBrush="{x:Null}"
                BorderThickness="0"
                ItemsSource="{Binding VariationList}"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="BorderThickness" Value="0" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <ToggleButton
                                x:Name="ToggleButton"
                                Click="OnEmojiPicked"
                                DataContext="{Binding}"
                                Style="{Binding ElementName=UserControl_Root, Path=EmojiToggleButtonStyle}">
                                <Image Height="{Binding EmojiItemSize, ElementName=UserControl_Root}" emoji:EmojiImage.Source="{Binding Path=Text}" />
                            </ToggleButton>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ControlTemplate>

        <DataTemplate x:Key="CellTemplate">
            <ToggleButton
                x:Name="VariationButton"
                Click="OnEmojiPicked"
                Focusable="False"
                IsHitTestVisible="{Binding ElementName=VariationPopup, Path=IsOpen, Mode=OneWay, Converter={stfu:BoolInverter}}"
                Style="{Binding ElementName=UserControl_Root, Path=EmojiToggleButtonStyle}"
                ToolTip="{Binding Path=DisplayName}">
                <Grid>
                    <Image
                        Height="{Binding EmojiItemSize, ElementName=UserControl_Root}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        emoji:EmojiImage.Source="{Binding Path=Text}" />
                    <Path
                        Width="6"
                        Height="6"
                        Margin="1"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Data="M478.058 762.952L89.372 374.264c-18.746-18.746-18.746-49.138 0-67.882l45.334-45.334c18.714-18.714 49.044-18.75 67.802-0.08L512 569.01l309.49-308.042c18.758-18.67 49.088-18.634 67.802 0.08l45.334 45.334c18.746 18.746 18.746 49.138 0 67.882L545.942 762.952c-18.746 18.744-49.138 18.744-67.884 0z"
                        Fill="{Binding ElementName=UserControl_Root, Path=Foreground}"
                        Stretch="Uniform"
                        Visibility="{Binding HasVariations, Converter={StaticResource BoolToVis}}" />
                    <Popup
                        x:Name="VariationPopup"
                        Margin="0"
                        AllowsTransparency="True"
                        IsOpen="{Binding IsChecked, ElementName=VariationButton, Mode=TwoWay}"
                        StaysOpen="False">
                        <Border Style="{Binding PopupBorderStyle, ElementName=UserControl_Root}">
                            <ContentControl Template="{StaticResource ResourceKey=VariationPopupTemplate}" />
                        </Border>
                    </Popup>
                </Grid>
            </ToggleButton>
        </DataTemplate>

    </FrameworkElement.Resources>

    <TabControl ItemsSource="{Binding EmojiGroups, ElementName=UserControl_Root}">
        <TabControl.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Image Height="24" emoji:EmojiImage.Source="{Binding Icon}">
                        <Image.ToolTip>
                            <TextBlock Text="{Binding Name}" />
                        </Image.ToolTip>
                    </Image>
                </Grid>
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <TextBlock
                        Margin="4"
                        FontStyle="Italic"
                        Text="{Binding Name}" />
                    <ListBox
                        Name="EmojiListView"
                        Grid.Row="1"
                        Margin="1"
                        Padding="0"
                        Background="Transparent"
                        BorderBrush="{x:Null}"
                        BorderThickness="0"
                        ItemsSource="{Binding EmojiChunkList}"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Visible"
                        VirtualizingStackPanel.IsVirtualizing="True"
                        VirtualizingStackPanel.VirtualizationMode="Recycling">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="{x:Type ListBoxItem}">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                            <ContentPresenter />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <ItemsControl ItemTemplate="{StaticResource ResourceKey=CellTemplate}" ItemsSource="{Binding}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</UserControl>
