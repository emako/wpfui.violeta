<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Design.TypographyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:lang="clr-namespace:Wpf.Ui.Violeta.Gallery.Globalization"
    Title="{I18N {x:Static lang:LangKeys.Gallery_Page_Typography}}"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock Margin="0,0,0,16" FontSize="20" FontWeight="SemiBold" Text="{I18N {x:Static lang:LangKeys.Gallery_Page_Typography}}" />
            <Border Margin="0,0,0,16" Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="10">
                <StackPanel>
                    <TextBlock FontSize="32" FontWeight="Bold" Text="{I18N {x:Static lang:LangKeys.Sample_b9987a246a}}" />
                    <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Text="{I18N {x:Static lang:LangKeys.Sample_34657fd293}}" />
                    <TextBlock Margin="0,8,0,0" FontSize="24" FontWeight="SemiBold" Text="{I18N {x:Static lang:LangKeys.Sample_b652de80d6}}" />
                    <TextBlock Margin="0,8,0,0" FontSize="20" FontWeight="SemiBold" Text="{I18N {x:Static lang:LangKeys.Sample_035f4e29da}}" />
                    <TextBlock Margin="0,8,0,0" FontSize="14" Text="{I18N {x:Static lang:LangKeys.Sample_d8c6ee30ab}}" />
                    <TextBlock Margin="0,8,0,0" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{I18N {x:Static lang:LangKeys.Sample_9dd3ceb5b3}}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
