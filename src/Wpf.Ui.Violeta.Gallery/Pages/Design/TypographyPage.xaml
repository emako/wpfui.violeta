<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Design.TypographyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:ctrl="clr-namespace:Wpf.Ui.Violeta.Gallery.Controls"
    Title="Typography"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock Margin="0,0,0,16" FontSize="20" FontWeight="SemiBold" Text="Typography" />
            <Border Margin="0,0,0,16" Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="10">
                <StackPanel>
                    <TextBlock FontSize="32" FontWeight="Bold" Text="Display" />
                    <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Text="Title Large" />
                    <TextBlock Margin="0,8,0,0" FontSize="24" FontWeight="SemiBold" Text="Title" />
                    <TextBlock Margin="0,8,0,0" FontSize="20" FontWeight="SemiBold" Text="Subtitle" />
                    <TextBlock Margin="0,8,0,0" FontSize="14" Text="Body — 正文内容示例文字" />
                    <TextBlock Margin="0,8,0,0" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Caption — 辅助说明文字" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
