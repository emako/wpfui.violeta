<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Design.ColorsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    Title="Colors"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock Margin="0,0,0,16" FontSize="20" FontWeight="SemiBold" Text="Colors" />
            <Border Margin="0,0,0,16" Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="10">
                <StackPanel>
                    <TextBlock Margin="0,0,0,12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="主题画刷资源示例。" />
                    <StackPanel>
                        <Border Height="36" Margin="0,0,0,8" CornerRadius="4" Background="{DynamicResource AccentFillColorDefaultBrush}" />
                        <Border Height="36" Margin="0,0,0,8" CornerRadius="4" Background="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" />
                        <Border Height="36" Margin="0,0,0,8" CornerRadius="4" Background="{DynamicResource ControlFillColorSecondaryBrush}" />
                        <Border Height="36" Margin="0,0,0,8" CornerRadius="4" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1" />
                        <Border Height="36" CornerRadius="4" Background="{DynamicResource SystemFillColorSuccessBrush}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
