<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Collections.TreeListViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    Title="TreeListView"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">

            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="TreeListView" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="TreeListView — 树形列表视图" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="结合树形结构和列表视图，支持展开/折叠和多列显示。" />
                    <ui:TreeListView
                        x:Name="StaffTreeListView"
                        Height="280"
                        ItemsSource="{Binding StaffList}">
                        <ui:TreeListView.Columns>
                            <GridViewColumnCollection>
                                <ui:GridViewColumn Width="280" Header="姓名">
                                    <ui:GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ui:TreeRowExpander Content="{Binding Name}" />
                                        </DataTemplate>
                                    </ui:GridViewColumn.CellTemplate>
                                </ui:GridViewColumn>
                                <ui:GridViewColumn
                                    Width="80"
                                    DisplayMemberBinding="{Binding Age}"
                                    Header="年龄" />
                                <ui:GridViewColumn
                                    Width="80"
                                    DisplayMemberBinding="{Binding Sex}"
                                    Header="性别" />
                                <ui:GridViewColumn
                                    Width="120"
                                    DisplayMemberBinding="{Binding Duty}"
                                    Header="职位" />
                                <ui:GridViewColumn Width="100" Header="状态">
                                    <ui:GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ui:ToggleSwitch IsChecked="{Binding IsChecked}" />
                                        </DataTemplate>
                                    </ui:GridViewColumn.CellTemplate>
                                </ui:GridViewColumn>
                            </GridViewColumnCollection>
                        </ui:TreeListView.Columns>
                        <ui:TreeListView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding StaffList}" />
                        </ui:TreeListView.ItemTemplate>
                    </ui:TreeListView>
                    <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
                        <ui:Button Click="AddStaffNode_Click" Content="添加节点" />
                        <ui:Button
                            Margin="8,0,0,0"
                            Click="ChangeStaffNode_Click"
                            Content="修改节点" />
                    </StackPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</vio:Page>
