<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Collections.TreeModelListViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    Title="TreeModelListView"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">

            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="TreeModelListView" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="TreeModelListView — 树模型列表视图" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="基于 ITreeModel 接口的树形列表，支持动态加载和自定义数据模型。" />
                    <ui:TreeModelListView Height="240" Model="{Binding TreeTestModel}">
                        <ui:GridView>
                            <ui:GridView.Columns>
                                <ui:GridViewColumn Width="280" Header="列1">
                                    <ui:GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ui:TreeModelRowExpander Content="{Binding Column1}" />
                                        </DataTemplate>
                                    </ui:GridViewColumn.CellTemplate>
                                </ui:GridViewColumn>
                                <ui:GridViewColumn
                                    Width="200"
                                    DisplayMemberBinding="{Binding Column2}"
                                    Header="列2" />
                                <ui:GridViewColumn
                                    Width="200"
                                    DisplayMemberBinding="{Binding Column3}"
                                    Header="列3" />
                                <ui:GridViewColumn Width="100" Header="选中">
                                    <ui:GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ui:ToggleSwitch IsChecked="{Binding IsChecked}" />
                                        </DataTemplate>
                                    </ui:GridViewColumn.CellTemplate>
                                </ui:GridViewColumn>
                            </ui:GridView.Columns>
                        </ui:GridView>
                    </ui:TreeModelListView>
                    <StackPanel Margin="0,8,0,0" Orientation="Horizontal">
                        <ui:Button Click="AddTreeModelRow_Click" Content="添加行" />
                        <ui:Button
                            Margin="8,0,0,0"
                            Click="RemoveTreeModelRow_Click"
                            Content="删除行" />
                        <ui:Button
                            Margin="8,0,0,0"
                            Click="ClearTreeModel_Click"
                            Content="清空" />
                    </StackPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</vio:Page>
