<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Collections.ListViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    Title="ListView"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">

            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="ListView" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="ListView — 列表视图" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="Fluent 风格 ListView，配合 GridView 显示多列。" />
                    <ui:ListView MaxHeight="320">
                        <ui:ListView.View>
                            <ui:GridView>
                                <ui:GridViewColumn Width="160" DisplayMemberBinding="{Binding}" Header="名称" />
                                <ui:GridViewColumn Width="120" Header="类型" />
                                <ui:GridViewColumn Width="100" Header="状态" />
                            </ui:GridView>
                        </ui:ListView.View>
                        <ui:ListViewItem>
                            <ui:ListViewItem.Content>
                                <TextBlock Text="文档.docx" />
                            </ui:ListViewItem.Content>
                        </ui:ListViewItem>
                        <ui:ListViewItem>
                            <ui:ListViewItem.Content>
                                <TextBlock Text="设计稿.fig" />
                            </ui:ListViewItem.Content>
                        </ui:ListViewItem>
                        <ui:ListViewItem>
                            <ui:ListViewItem.Content>
                                <TextBlock Text="发布说明.md" />
                            </ui:ListViewItem.Content>
                        </ui:ListViewItem>
                        <ui:ListViewItem>
                            <ui:ListViewItem.Content>
                                <TextBlock Text="截图.png" />
                            </ui:ListViewItem.Content>
                        </ui:ListViewItem>
                    </ui:ListView>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</vio:Page>
