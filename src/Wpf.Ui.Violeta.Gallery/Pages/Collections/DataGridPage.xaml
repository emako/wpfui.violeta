<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Collections.DataGridPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:lang="clr-namespace:Wpf.Ui.Violeta.Gallery.Globalization"
    Title="DataGrid"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">

            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="DataGrid" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="{I18N {x:Static lang:LangKeys.Sample_976db78a60}}" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{I18N {x:Static lang:LangKeys.Sample_947e334a08}}" />
                    <ui:DataGrid
                        x:Name="SampleDataGrid"
                        MaxHeight="360"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        IsReadOnly="True">
                        <ui:DataGrid.Columns>
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Name}"
                                Header="{I18N {x:Static lang:LangKeys.Sample_60d0458ac6}}" />
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Department}"
                                Header="{I18N {x:Static lang:LangKeys.Sample_1e1459eeed}}" />
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Score}"
                                Header="{I18N {x:Static lang:LangKeys.Sample_b34422e637}}" />
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Status}"
                                Header="{I18N {x:Static lang:LangKeys.Sample_3fea7ca76c}}" />
                        </ui:DataGrid.Columns>
                    </ui:DataGrid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</vio:Page>
