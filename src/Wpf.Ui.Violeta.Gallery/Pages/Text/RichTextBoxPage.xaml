<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Text.RichTextBoxPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:lang="clr-namespace:Wpf.Ui.Violeta.Gallery.Globalization"
    Title="RichTextBox"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="RichTextBox" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="{I18N {x:Static lang:LangKeys.Sample_8c56954140}}" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{I18N {x:Static lang:LangKeys.Sample_f91458ff7d}}" />
                    <RichTextBox
                        Height="160"
                        HorizontalAlignment="Stretch">
                        <FlowDocument>
                            <Paragraph>
                                <Run FontWeight="SemiBold" Text="{I18N {x:Static lang:LangKeys.Sample_da3e0a9d70}}"></Run>
                                <LineBreak />
                                <Run Text="{I18N {x:Static lang:LangKeys.Sample_0b8cd339bb}}"></Run>
                                <Run FontStyle="Italic" Text="{I18N {x:Static lang:LangKeys.Sample_f6d51bcb81}}"></Run>
                                <Run Text="{I18N {x:Static lang:LangKeys.Sample_932b484438}}"></Run>
                            </Paragraph>
                            <Paragraph>
                                <Run Text="{I18N {x:Static lang:LangKeys.Sample_262b0b07df}}"></Run>
                                <Run FontWeight="Bold" Text="{I18N {x:Static lang:LangKeys.Sample_e803663cf5}}"></Run>
                                <Run>、</Run>
                                <Run FontStyle="Italic" Text="{I18N {x:Static lang:LangKeys.Sample_81d6da77d6}}"></Run>
                                <Run Text="{I18N {x:Static lang:LangKeys.Sample_3b04598bd0}}"></Run>
                                <Run TextDecorations="Underline" Text="{I18N {x:Static lang:LangKeys.Sample_f6f8ea0815}}"></Run>
                                <Run Text="{I18N {x:Static lang:LangKeys.Sample_a69a0f6783}}"></Run>
                            </Paragraph>
                        </FlowDocument>
                    </RichTextBox>
                </StackPanel>
            </Border>

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="ui:RichTextBox" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{I18N {x:Static lang:LangKeys.Sample_46e283835a}}" />
                    <ui:RichTextBox Height="120" HorizontalAlignment="Stretch">
                        <FlowDocument>
                            <Paragraph>
                                <Run>Wpf.Ui RichTextBox with FlowDocument content.</Run>
                            </Paragraph>
                        </FlowDocument>
                    </ui:RichTextBox>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
