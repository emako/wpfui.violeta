<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Text.BoolStateTextBlockPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    Title="BoolStateTextBlock"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="BoolStateTextBlock" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="布尔状态文本" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="根据 Value 在 TrueText / FalseText 间切换，并可分别设置字体样式。" />
                    <ui:StackPanel Orientation="Horizontal" Spacing="16">
                        <vio:ToggleSwitch
                            x:Name="StateToggle"
                            Header="状态"
                            IsOn="True" />
                        <vio:BoolStateTextBlock
                            VerticalAlignment="Center"
                            FalseText="Offline"
                            TrueText="Online"
                            Value="{Binding IsOn, ElementName=StateToggle}">
                            <vio:BoolStateTextBlock.TrueFontOptions>
                                <vio:BoolStateTextBlockFontOptions
                                    FontSize="16"
                                    FontWeight="SemiBold"
                                    Foreground="Red" />
                            </vio:BoolStateTextBlock.TrueFontOptions>
                            <vio:BoolStateTextBlock.FalseFontOptions>
                                <vio:BoolStateTextBlockFontOptions
                                    FontSize="14"
                                    FontWeight="Normal"
                                    Foreground="Blue"
                                    TextDecorations="{x:Static TextDecorations.Underline}" />
                            </vio:BoolStateTextBlock.FalseFontOptions>
                        </vio:BoolStateTextBlock>
                    </ui:StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
