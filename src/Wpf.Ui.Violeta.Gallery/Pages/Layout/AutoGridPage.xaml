<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Layout.AutoGridPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    Title="AutoGrid"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">

            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="AutoGrid" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="AutoGrid — 2×2" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="按子元素顺序自动填充行列。" />
                    <ui:AutoGrid
                        ColumnCount="2"
                        RowCount="2"
                        RowHeight="40">
                        <TextBlock VerticalAlignment="Center" Text="Title1" />
                        <ui:Button Content="Button1" />
                        <TextBlock VerticalAlignment="Center" Text="Title2" />
                        <ui:Button Content="Button2" />
                    </ui:AutoGrid>
                </StackPanel>
            </Border>

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="ChildMargin 示例" />
                    <ui:AutoGrid
                        Margin="0,8,0,0"
                        ChildMargin="9"
                        ColumnCount="3"
                        RowCount="2">
                        <Border Width="30" Height="30" Background="Red" />
                        <Border Width="30" Height="30" Background="Green" />
                        <Border Width="30" Height="30" Background="Red" />
                        <Border Width="30" Height="30" Background="Green" />
                        <Border Width="30" Height="30" Background="Red" />
                        <Border Width="30" Height="30" Background="Green" />
                    </ui:AutoGrid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</vio:Page>
