<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Layout.DropShadowChromePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:lang="clr-namespace:Wpf.Ui.Violeta.Gallery.Globalization"
    Title="DropShadowChrome"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">

            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="DropShadowChrome" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="{I18N {x:Static lang:LangKeys.Sample_877990def9}}" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{I18N {x:Static lang:LangKeys.Sample_5148eb7ab0}}" />
                    <WrapPanel>
                        <ui:DropShadowChrome Margin="0,0,16,16" CornerRadius="0">
                            <Border
                                Width="80"
                                Height="50"
                                Background="{DynamicResource ApplicationBackgroundBrush}" />
                        </ui:DropShadowChrome>
                        <ui:DropShadowChrome Margin="0,0,16,16" CornerRadius="8">
                            <Border
                                Width="80"
                                Height="50"
                                Background="{DynamicResource ApplicationBackgroundBrush}"
                                CornerRadius="8" />
                        </ui:DropShadowChrome>
                        <ui:DropShadowChrome
                            Margin="0,0,16,16"
                            CornerRadius="4"
                            Color="#C0FF0000">
                            <Border
                                Width="80"
                                Height="50"
                                Background="{DynamicResource ApplicationBackgroundBrush}"
                                CornerRadius="4" />
                        </ui:DropShadowChrome>
                    </WrapPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</vio:Page>
