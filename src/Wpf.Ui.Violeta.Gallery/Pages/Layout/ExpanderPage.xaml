<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Layout.ExpanderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:lang="clr-namespace:Wpf.Ui.Violeta.Gallery.Globalization"
    Title="{I18N {x:Static lang:LangKeys.Gallery_Page_Expander}}"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="{I18N {x:Static lang:LangKeys.Gallery_Page_Expander}}" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="{I18N {x:Static lang:LangKeys.Sample_2749822f6c}}" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{I18N {x:Static lang:LangKeys.Sample_9c3760d853}}" />
                    <Expander Header="{I18N {x:Static lang:LangKeys.Sample_9ad3b66421}}" IsExpanded="True">
                        <TextBlock
                            Margin="0,8,0,0"
                            Text="{I18N {x:Static lang:LangKeys.Sample_3dc186229d}}"
                            TextWrapping="Wrap" />
                    </Expander>
                    <Expander Margin="0,12,0,0" Header="{I18N {x:Static lang:LangKeys.Sample_28c448376e}}">
                        <StackPanel Margin="0,8,0,0">
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_e04cead454}}" />
                            <TextBlock Margin="0,4,0,0" Text="{I18N {x:Static lang:LangKeys.Sample_d581ed5da9}}" />
                            <TextBlock Margin="0,4,0,0" Text="{I18N {x:Static lang:LangKeys.Sample_cbae0a2a40}}" />
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
