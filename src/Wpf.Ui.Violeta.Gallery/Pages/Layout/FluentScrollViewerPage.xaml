<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Layout.FluentScrollViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:lang="clr-namespace:Wpf.Ui.Violeta.Gallery.Globalization"
    Title="FluentScrollViewer"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="FluentScrollViewer" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="{I18N {x:Static lang:LangKeys.Sample_a96498c010}}" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{I18N {x:Static lang:LangKeys.Sample_3d502b523f}}" />
                    <vio:FluentScrollViewer Height="160" BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                        <StackPanel Margin="12">
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_a6e8671eab}}" Margin="0,0,0,8" />
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_0fa99936c9}}" Margin="0,0,0,8" />
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_899eaed4fd}}" Margin="0,0,0,8" />
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_e970cf7899}}" Margin="0,0,0,8" />
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_cd7eb7bcf9}}" Margin="0,0,0,8" />
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_44768e6912}}" Margin="0,0,0,8" />
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_73709b070d}}" Margin="0,0,0,8" />
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_1f7768a40c}}" Margin="0,0,0,8" />
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_4d561128b7}}" Margin="0,0,0,8" />
                            <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_aa3eb7c441}}" />
                        </StackPanel>
                    </vio:FluentScrollViewer>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
