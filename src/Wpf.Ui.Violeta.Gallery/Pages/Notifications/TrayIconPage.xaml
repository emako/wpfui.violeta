<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Notifications.TrayIconPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:lang="clr-namespace:Wpf.Ui.Violeta.Gallery.Globalization"
    Title="TrayIcon"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">

            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="TrayIcon" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="{I18N {x:Static lang:LangKeys.Sample_0b125f41f7}}" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        TextWrapping="Wrap"
                        Text="{I18N {x:Static lang:LangKeys.Sample_5d2567470b}}" />
                    <TextBlock
                        Margin="0,0,0,8"
                        Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                        TextWrapping="Wrap"
                        Text="{I18N {x:Static lang:LangKeys.Sample_cfd0ca6826}}" />
                    <WrapPanel>
                        <ui:Button
                            Margin="0,0,8,8"
                            Appearance="Primary"
                            Click="ShowTrayBalloon_Click"
                            Content="{I18N {x:Static lang:LangKeys.Sample_1b0e13a022}}" />
                        <ui:Button
                            Margin="0,0,8,8"
                            Click="HideToTray_Click"
                            Content="{I18N {x:Static lang:LangKeys.Sample_7e0978814d}}" />
                        <ui:Button
                            Margin="0,0,8,8"
                            Click="ShowFromTray_Click"
                            Content="{I18N {x:Static lang:LangKeys.Sample_ca624f0706}}" />
                    </WrapPanel>
                    <ui:ToggleSwitch
                        x:Name="TwinkToggle"
                        Margin="0,8,0,0"
                        HorizontalAlignment="Left"
                        Content="Twink"
                        Checked="TwinkToggle_OnChanged"
                        Unchecked="TwinkToggle_OnChanged" />
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</vio:Page>
