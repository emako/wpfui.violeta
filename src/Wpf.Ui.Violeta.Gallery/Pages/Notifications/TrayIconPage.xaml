<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Notifications.TrayIconPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    Title="TrayIcon"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">

            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="TrayIcon" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="系统托盘图标" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        TextWrapping="Wrap"
                        Text="TrayIconHost / TrayMenuItem 用于在系统通知区域显示图标与右键菜单。完整示例见 Wpf.Ui.Test 的 TrayIconManager；Gallery 以 Toast 模拟托盘通知交互。" />
                    <TextBlock
                        Margin="0,0,0,8"
                        Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                        TextWrapping="Wrap"
                        Text="典型用法：创建 TrayIconHost，设置 Icon / ToolTipText / Menu，并处理 LeftDoubleClick 以还原主窗口。" />
                    <WrapPanel>
                        <ui:Button
                            Margin="0,0,8,8"
                            Click="SimulateTrayBalloon_Click"
                            Content="模拟托盘气球通知" />
                        <ui:Button
                            Margin="0,0,8,8"
                            Click="SimulateTrayMenu_Click"
                            Content="模拟菜单操作提示" />
                    </WrapPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</vio:Page>
