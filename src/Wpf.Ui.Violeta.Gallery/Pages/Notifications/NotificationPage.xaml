<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Notifications.NotificationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    Title="Notification"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">

            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="Notification" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="BalloonTip" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        TextWrapping="Wrap"
                        Text="Static helpers similar to Toast. Uses a hidden tray icon internally, so apps do not need to host TrayIconHost." />
                    <WrapPanel>
                        <ui:Button
                            Margin="0,0,8,8"
                            Appearance="Primary"
                            Click="ShowInformation_Click"
                            Content="Information" />
                        <ui:Button
                            Margin="0,0,8,8"
                            Click="ShowWarning_Click"
                            Content="Warning" />
                        <ui:Button
                            Margin="0,0,8,8"
                            Click="ShowError_Click"
                            Content="Error" />
                        <ui:Button
                            Margin="0,0,8,8"
                            Click="ShowNone_Click"
                            Content="None" />
                    </WrapPanel>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</vio:Page>
