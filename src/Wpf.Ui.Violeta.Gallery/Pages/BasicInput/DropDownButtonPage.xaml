<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.BasicInput.DropDownButtonPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:lang="clr-namespace:Wpf.Ui.Violeta.Gallery.Globalization"
    Title="DropDownButton"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="DropDownButton" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="DropDownButton" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{I18N {x:Static lang:LangKeys.Sample_e44ca4d5c0}}" />
                    <vio:DropDownButton Content="Hello" Icon="{ui:SymbolIcon Fluent24}">
                        <vio:DropDownButton.Flyout>
                            <ContextMenu>
                                <MenuItem Header="Add" />
                                <MenuItem Header="Remove" />
                                <MenuItem Header="Send">
                                    <MenuItem Header="As email" />
                                    <MenuItem Header="As file" />
                                    <MenuItem Header="To device">
                                        <MenuItem Header="Phone" />
                                        <MenuItem Header="Tablet" />
                                        <MenuItem Header="Desktop" />
                                    </MenuItem>
                                </MenuItem>
                                <MenuItem Header="Hello" />
                            </ContextMenu>
                        </vio:DropDownButton.Flyout>
                    </vio:DropDownButton>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
