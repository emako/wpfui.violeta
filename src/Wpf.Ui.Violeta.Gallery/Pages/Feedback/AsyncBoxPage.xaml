<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Feedback.AsyncBoxPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    Title="AsyncBox"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock Margin="0,0,0,16" FontSize="20" FontWeight="SemiBold" Text="AsyncBox" />
            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock Margin="0,0,0,4" FontSize="14" FontWeight="SemiBold" Text="AsyncBox — 异步内容" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="在独立 UI 线程异步渲染子内容，加载过程中可显示 Loading 视图。"
                        TextWrapping="Wrap" />
                    <Border
                        MinHeight="80"
                        Padding="12"
                        Background="{DynamicResource ControlFillColorDefaultBrush}"
                        CornerRadius="6">
                        <vio:AsyncBox LoadingDelay="1" LoadingViewType="{x:Type vio:Loading}">
                            <TextBlock
                                TextWrapping="Wrap"
                                Text="这是 AsyncBox 演示内容。子内容在异步 UI 线程中创建，加载时可显示指示器。" />
                        </vio:AsyncBox>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
