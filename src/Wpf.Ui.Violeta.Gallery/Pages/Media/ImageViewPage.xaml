<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Media.ImageViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    Title="ImageView"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock Margin="0,0,0,16" FontSize="20" FontWeight="SemiBold" Text="ImageView" />
            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock Margin="0,0,0,4" FontSize="14" FontWeight="SemiBold" Text="ImageView — 图片查看器" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="支持缩放与平移。Source 可使用 pack URI 或相对资源路径。"
                        TextWrapping="Wrap" />
                    <ui:Border
                        Width="320"
                        Height="320"
                        HorizontalAlignment="Left"
                        Background="{DynamicResource ControlFillColorDefaultBrush}"
                        ClipToBounds="True"
                        CornerRadius="8">
                        <vio:ImageView Source="pack://application:,,,/Wpf.Ui.Violeta.Gallery;component/Resources/Images/wpfui.png" />
                    </ui:Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
