<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Navigation.NavigationViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:lang="clr-namespace:Wpf.Ui.Violeta.Gallery.Globalization"
    Title="{I18N {x:Static lang:LangKeys.Gallery_Page_NavigationView}}"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">

            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="{I18N {x:Static lang:LangKeys.Gallery_Page_NavigationView}}" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="{I18N {x:Static lang:LangKeys.Sample_064a169925}}" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        TextWrapping="Wrap"
                        Text="{I18N {x:Static lang:LangKeys.Sample_b40b2d467a}}" />

                    <TextBlock Margin="0,0,0,6" FontWeight="SemiBold" Text="{I18N {x:Static lang:LangKeys.Sample_285f2d65c2}}" />
                    <TextBlock Margin="0,0,0,4" TextWrapping="Wrap" Text="{I18N {x:Static lang:LangKeys.Sample_e080e01adc}}" />
                    <TextBlock Margin="0,0,0,4" TextWrapping="Wrap" Text="{I18N {x:Static lang:LangKeys.Sample_0674868522}}" />
                    <TextBlock Margin="0,0,0,4" TextWrapping="Wrap" Text="{I18N {x:Static lang:LangKeys.Sample_222c7173fa}}" />
                    <TextBlock Margin="0,0,0,4" TextWrapping="Wrap" Text="{I18N {x:Static lang:LangKeys.Sample_d359e84325}}" />
                    <TextBlock Margin="0,0,0,12" TextWrapping="Wrap" Text="{I18N {x:Static lang:LangKeys.Sample_f07479f3ac}}" />

                    <TextBlock Margin="0,0,0,6" FontWeight="SemiBold" Text="{I18N {x:Static lang:LangKeys.Sample_de882bfd0a}}" />
                    <TextBlock Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{I18N {x:Static lang:LangKeys.Sample_9bdc3ea41f}}" />
                    <TextBlock Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{I18N {x:Static lang:LangKeys.Sample_6e9b5e7aa8}}" />
                    <TextBlock Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{I18N {x:Static lang:LangKeys.Sample_aabb3f6de1}}" />
                    <TextBlock Margin="0,0,0,4" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{I18N {x:Static lang:LangKeys.Sample_d229e6f892}}" />
                    <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="{I18N {x:Static lang:LangKeys.Sample_0144f29a17}}" />
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</vio:Page>
