<vio:Page
    x:Class="Wpf.Ui.Violeta.Gallery.Pages.Navigation.TabViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
    xmlns:lang="clr-namespace:Wpf.Ui.Violeta.Gallery.Globalization"
    Title="{I18N {x:Static lang:LangKeys.Gallery_Page_TabView}}"
    d:Background="#1C1C1C"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Margin="24,16,24,24">
            <TextBlock
                Margin="0,0,0,16"
                FontSize="20"
                FontWeight="SemiBold"
                Text="{I18N {x:Static lang:LangKeys.Gallery_Page_TabView}}" />

            <Border
                Margin="0,0,0,16"
                Padding="20"
                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="10">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Text="{I18N {x:Static lang:LangKeys.Sample_56b9e13d5e}}" />
                    <TextBlock
                        Margin="0,0,0,12"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="{I18N {x:Static lang:LangKeys.Sample_39886cefd9}}"
                        TextWrapping="Wrap" />
                    <ui:TabView Margin="0,8,0,0">
                        <ui:TabViewItem>
                            <ui:TabViewItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Margin="0,0,6,0" Symbol="XboxConsole24" />
                                    <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_8b1a9953c4}}" />
                                </StackPanel>
                            </ui:TabViewItem.Header>
                            <Grid>
                                <TextBlock Margin="12" Text="{I18N {x:Static lang:LangKeys.Sample_f5a7924e62}}" />
                            </Grid>
                        </ui:TabViewItem>
                        <ui:TabViewItem IsSelected="True">
                            <ui:TabViewItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Margin="0,0,6,0" Symbol="StoreMicrosoft16" />
                                    <TextBlock Text="{I18N {x:Static lang:LangKeys.Sample_4b111b983d}}" />
                                </StackPanel>
                            </ui:TabViewItem.Header>
                            <Grid>
                                <TextBlock Margin="12" Text="{I18N {x:Static lang:LangKeys.Sample_5c4f2fcaf6}}" />
                            </Grid>
                        </ui:TabViewItem>
                    </ui:TabView>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</vio:Page>
