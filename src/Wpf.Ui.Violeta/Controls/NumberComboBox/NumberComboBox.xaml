<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
    xmlns:local="clr-namespace:Wpf.Ui.Violeta.Controls"
    xmlns:ui="clr-namespace:Wpf.Ui.Controls">

    <!--
        NumberComboBox reuses ComboBox chrome (editable text + chevron),
        with optional InnerLeftContent / InnerRightContent for units.
        IsEditable is coerced to True; item selection is optional.
    -->
    <Style
        x:Key="DefaultNumberComboBoxStyle"
        BasedOn="{StaticResource DefaultComboBoxStyle}"
        TargetType="{x:Type local:NumberComboBox}">
        <Setter Property="IsEditable" Value="True" />
        <Setter Property="IsTextSearchEnabled" Value="False" />
        <!--  Popup items mirror InnerLeft/Right so units appear next to each value.  -->
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <ContentControl
                            VerticalAlignment="Center"
                            Content="{Binding InnerLeftContent, RelativeSource={RelativeSource AncestorType={x:Type local:NumberComboBox}}}"
                            Focusable="False">
                            <ContentControl.Style>
                                <Style TargetType="{x:Type ContentControl}">
                                    <Setter Property="Margin" Value="0,0,6,0" />
                                    <Style.Triggers>
                                        <DataTrigger
                                            Binding="{Binding InnerLeftContent, RelativeSource={RelativeSource AncestorType={x:Type local:NumberComboBox}}}"
                                            Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ContentControl.Style>
                        </ContentControl>
                        <ContentPresenter VerticalAlignment="Center" Content="{Binding}" />
                        <ContentControl
                            VerticalAlignment="Center"
                            Content="{Binding InnerRightContent, RelativeSource={RelativeSource AncestorType={x:Type local:NumberComboBox}}}"
                            Focusable="False">
                            <ContentControl.Style>
                                <Style TargetType="{x:Type ContentControl}">
                                    <Setter Property="Margin" Value="6,0,0,0" />
                                    <Style.Triggers>
                                        <DataTrigger
                                            Binding="{Binding InnerRightContent, RelativeSource={RelativeSource AncestorType={x:Type local:NumberComboBox}}}"
                                            Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ContentControl.Style>
                        </ContentControl>
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NumberComboBox}">
                    <Grid HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalAlignment}">
                        <Border
                            x:Name="ContentBorder"
                            Grid.Row="0"
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            MinWidth="{TemplateBinding MinWidth}"
                            MinHeight="{TemplateBinding MinHeight}"
                            Padding="0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}">
                            <Grid HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!--  InnerLeftContent (unit / prefix)  -->
                                    <ContentPresenter
                                        x:Name="PART_InnerLeftContent"
                                        Grid.Column="0"
                                        Margin="10,0,0,0"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding InnerLeftContent}"
                                        IsHitTestVisible="False"
                                        TextElement.Foreground="{TemplateBinding Foreground}"
                                        Visibility="Collapsed" />

                                    <Grid Grid.Column="1" Margin="{TemplateBinding Padding}">
                                        <TextBlock
                                            x:Name="PART_Placeholder"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Center"
                                            FontSize="{TemplateBinding FontSize}"
                                            Foreground="{DynamicResource TextControlPlaceholderForeground}"
                                            IsHitTestVisible="False"
                                            Text="{Binding Path=(ui:ControlHelper.PlaceholderText), RelativeSource={RelativeSource TemplatedParent}}"
                                            TextTrimming="CharacterEllipsis"
                                            Visibility="Collapsed" />
                                        <ContentPresenter
                                            Name="PART_ContentPresenter"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            IsHitTestVisible="False"
                                            TextElement.Foreground="{TemplateBinding Foreground}" />
                                    </Grid>

                                    <!--  InnerRightContent (unit / suffix)  -->
                                    <ContentPresenter
                                        x:Name="PART_InnerRightContent"
                                        Grid.Column="2"
                                        Margin="0,0,0,0"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding InnerRightContent}"
                                        IsHitTestVisible="False"
                                        TextElement.Foreground="{TemplateBinding Foreground}"
                                        Visibility="Collapsed" />

                                    <Grid Grid.Column="3" Margin="{StaticResource ComboBoxChevronMargin}">
                                        <controls:SymbolIcon
                                            x:Name="ChevronIcon"
                                            Margin="0"
                                            VerticalAlignment="Center"
                                            FontSize="{StaticResource ComboBoxChevronSize}"
                                            Foreground="{DynamicResource ComboBoxDropDownGlyphForeground}"
                                            RenderTransformOrigin="0.5, 0.5"
                                            Symbol="ChevronDown24">
                                            <controls:SymbolIcon.RenderTransform>
                                                <RotateTransform Angle="0" />
                                            </controls:SymbolIcon.RenderTransform>
                                        </controls:SymbolIcon>
                                    </Grid>

                                    <!--  Toggle covers all columns; editable text sits above content column only.  -->
                                    <Grid
                                        Grid.Column="0"
                                        Grid.ColumnSpan="4"
                                        Margin="0">
                                        <ToggleButton
                                            Name="ToggleButton"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            ClickMode="Press"
                                            Focusable="False"
                                            Foreground="{TemplateBinding Foreground}"
                                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                            Style="{StaticResource DefaultComboBoxToggleButtonStyle}" />
                                    </Grid>

                                    <Grid Grid.Column="1" Margin="{TemplateBinding Padding}">
                                        <controls:TextBox
                                            x:Name="PART_EditableTextBox"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            FontSize="{TemplateBinding FontSize}"
                                            Foreground="{TemplateBinding Foreground}"
                                            IsReadOnly="{TemplateBinding IsReadOnly}"
                                            PlaceholderText="{Binding Path=(ui:ControlHelper.PlaceholderText), RelativeSource={RelativeSource TemplatedParent}}"
                                            Style="{StaticResource DefaultComboBoxTextBoxStyle}" />
                                    </Grid>
                                </Grid>
                                <Popup
                                    x:Name="Popup"
                                    VerticalAlignment="Center"
                                    AllowsTransparency="True"
                                    Focusable="False"
                                    IsOpen="{TemplateBinding IsDropDownOpen}"
                                    Placement="{TemplateBinding Popup.Placement}"
                                    PlacementTarget="{Binding ElementName=ContentBorder}"
                                    PopupAnimation="{TemplateBinding Popup.PopupAnimation}"
                                    VerticalOffset="1">
                                    <controls:EffectThicknessDecorator
                                        AnimationDelay="00:00:00.167"
                                        AnimationElement="{Binding ElementName=DropDownBorder}"
                                        Thickness="30,0,30,30">
                                        <Border
                                            x:Name="DropDownBorder"
                                            MinWidth="{TemplateBinding ActualWidth}"
                                            Margin="0"
                                            Padding="0,4,0,6"
                                            Background="{DynamicResource ComboBoxDropDownBackground}"
                                            BorderBrush="{DynamicResource ComboBoxDropDownBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="{DynamicResource PopupCornerRadius}"
                                            SnapsToDevicePixels="True">
                                            <Border.RenderTransform>
                                                <TranslateTransform />
                                            </Border.RenderTransform>
                                            <Border.Effect>
                                                <DropShadowEffect
                                                    BlurRadius="20"
                                                    Direction="270"
                                                    Opacity="0.135"
                                                    ShadowDepth="10"
                                                    Color="#202020" />
                                            </Border.Effect>

                                            <Grid>
                                                <controls:DynamicScrollViewer
                                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                                    Margin="0"
                                                    HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                                    SnapsToDevicePixels="True"
                                                    TextElement.FontSize="{TemplateBinding FontSize}"
                                                    TextElement.FontWeight="{TemplateBinding FontWeight}"
                                                    TextElement.Foreground="{TemplateBinding Foreground}"
                                                    VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" TextElement.FontSize="{TemplateBinding FontSize}" />
                                                </controls:DynamicScrollViewer>
                                            </Grid>
                                        </Border>
                                    </controls:EffectThicknessDecorator>
                                </Popup>
                            </Grid>
                        </Border>
                        <Border
                            x:Name="AccentBorder"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            BorderBrush="{DynamicResource ComboBoxBorderBrushFocused}"
                            BorderThickness="{StaticResource ComboBoxAccentBorderThemeThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}"
                            Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ChevronIcon"
                                            Storyboard.TargetProperty="(controls:SymbolIcon.RenderTransform).(RotateTransform.Angle)"
                                            From="0"
                                            To="180"
                                            Duration="00:00:00.167" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="DropDownBorder"
                                            Storyboard.TargetProperty="(Border.RenderTransform).(TranslateTransform.Y)"
                                            From="-90"
                                            To="0"
                                            Duration="00:00:00.167">
                                            <DoubleAnimation.EasingFunction>
                                                <CircleEase EasingMode="EaseOut" />
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="ChevronIcon"
                                            Storyboard.TargetProperty="(controls:SymbolIcon.RenderTransform).(RotateTransform.Angle)"
                                            From="180"
                                            To="0"
                                            Duration="00:00:00.167" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="HasItems" Value="False">
                            <Setter TargetName="DropDownBorder" Property="MinHeight" Value="{StaticResource ComboBoxPopupMinHeight}" />
                        </Trigger>
                        <Trigger SourceName="Popup" Property="Popup.AllowsTransparency" Value="False">
                            <Setter TargetName="DropDownBorder" Property="CornerRadius" Value="0" />
                        </Trigger>
                        <Trigger Property="IsGrouping" Value="True">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="False" />
                        </Trigger>
                        <Trigger Property="SelectedItem" Value="{x:Null}">
                            <Setter TargetName="PART_Placeholder" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="False" />
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_ContentPresenter" Property="Visibility" Value="Hidden" />
                            <Setter TargetName="PART_Placeholder" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True" />
                                <Condition Property="IsKeyboardFocusWithin" Value="True" />
                                <Condition Property="IsEditable" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource ComboBoxBackgroundFocused}" />
                            <Setter TargetName="AccentBorder" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsKeyboardFocusWithin" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource ComboBoxBackgroundPointerOver}" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource ComboBoxBackgroundDisabled}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource ComboBoxBorderBrushDisabled}" />
                            <Setter TargetName="PART_Placeholder" Property="Foreground" Value="{DynamicResource ComboBoxForegroundDisabled}" />
                            <Setter Property="Foreground" Value="{DynamicResource ComboBoxForegroundDisabled}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberIntComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberUIntComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberDoubleComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberFloatComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberDecimalComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberLongComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberULongComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberShortComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberUShortComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberByteComboBox}" />
    <Style BasedOn="{StaticResource DefaultNumberComboBoxStyle}" TargetType="{x:Type local:NumberSByteComboBox}" />

</ResourceDictionary>
