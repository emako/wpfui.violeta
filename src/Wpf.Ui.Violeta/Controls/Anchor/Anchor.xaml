<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Wpf.Ui.Violeta.Controls">

    <local:AnchorLevelToMarginConverter x:Key="AnchorLevelToMarginConverter" />

    <!--  ===== AnchorItem =====  -->
    <Style x:Key="DefaultAnchorItemStyle" TargetType="{x:Type local:AnchorItem}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="8,5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:AnchorItem}">
                    <StackPanel>
                        <!--  Header row  -->
                        <Border
                            x:Name="PART_HeaderBorder"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4">
                            <Grid
                                Margin="{Binding Level,
                                                 RelativeSource={RelativeSource TemplatedParent},
                                                 Converter={StaticResource AnchorLevelToMarginConverter}}">
                                <Grid.ColumnDefinitions>
                                    <!--  Left accent bar  -->
                                    <ColumnDefinition Width="3" />
                                    <ColumnDefinition Width="6" />
                                    <!--  Text  -->
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!--  Accent indicator  -->
                                <Border
                                    x:Name="PART_Indicator"
                                    Grid.Column="0"
                                    Width="3"
                                    Height="16"
                                    Background="{DynamicResource SystemAccentColorBrush}"
                                    CornerRadius="2"
                                    Opacity="0" />

                                <!--  Header text  -->
                                <ContentPresenter
                                    x:Name="PART_Header"
                                    Grid.Column="2"
                                    VerticalAlignment="Center"
                                    ContentSource="Header"
                                    RecognizesAccessKey="True"
                                    TextElement.FontSize="13"
                                    TextElement.Foreground="{TemplateBinding Foreground}" />
                            </Grid>
                        </Border>

                        <!--  Sub-items  -->
                        <ItemsPresenter x:Name="PART_SubItems" />
                    </StackPanel>

                    <ControlTemplate.Triggers>
                        <!--  Hover  -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_HeaderBorder" Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                        </Trigger>

                        <!--  Selected  -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource SystemAccentColorBrush}" />
                            <Setter TargetName="PART_Indicator" Property="Opacity" Value="1" />
                            <Setter TargetName="PART_Header" Property="TextElement.FontWeight" Value="SemiBold" />
                        </Trigger>

                        <!--  Selected + hover  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_HeaderBorder" Property="Background" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultAnchorItemStyle}" TargetType="{x:Type local:AnchorItem}" />

    <!--  ===== Anchor (container) =====  -->
    <Style x:Key="DefaultAnchorStyle" TargetType="{x:Type local:Anchor}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Anchor}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ItemsPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultAnchorStyle}" TargetType="{x:Type local:Anchor}" />

</ResourceDictionary>
