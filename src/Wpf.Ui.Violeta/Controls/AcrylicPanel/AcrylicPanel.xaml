<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Wpf.Ui.Violeta.Controls">

    <!--  https://learn.microsoft.com/en-us/windows/apps/design/style/acrylic#usability-and-adaptability  -->
    <Style TargetType="local:AcrylicPanel">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="ClipToBounds" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:AcrylicPanel">
                    <Border ClipToBounds="True" CornerRadius="{TemplateBinding CornerRadius}">
                        <Grid>
                            <Border
                                x:Name="PART_BlurLayer"
                                Width="{Binding Target.ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                Height="{Binding Target.ActualHeight, RelativeSource={RelativeSource TemplatedParent}}"
                                IsHitTestVisible="False">
                                <Border.Background>
                                    <VisualBrush
                                        AlignmentX="Left"
                                        AlignmentY="Top"
                                        Stretch="None"
                                        ViewboxUnits="Absolute"
                                        Visual="{Binding Target, RelativeSource={RelativeSource TemplatedParent}}" />
                                </Border.Background>
                                <Border.Effect>
                                    <BlurEffect Radius="{Binding Amount, RelativeSource={RelativeSource TemplatedParent}}" />
                                </Border.Effect>
                            </Border>

                            <Border CornerRadius="{TemplateBinding CornerRadius}" Opacity="{TemplateBinding TintOpacity}">
                                <Border.Background>
                                    <SolidColorBrush Color="{Binding TintColor, RelativeSource={RelativeSource TemplatedParent}}" />
                                </Border.Background>
                            </Border>

                            <Border
                                CornerRadius="{TemplateBinding CornerRadius}"
                                IsHitTestVisible="False"
                                Opacity="{TemplateBinding NoiseOpacity}">
                                <Border.Background>
                                    <DrawingBrush
                                        Stretch="None"
                                        TileMode="Tile"
                                        Viewport="0,0,3,3"
                                        ViewportUnits="Absolute">
                                        <DrawingBrush.Drawing>
                                            <DrawingGroup>
                                                <GeometryDrawing Brush="#14FFFFFF" Geometry="M0,0 H1 V1 H0 Z" />
                                                <GeometryDrawing Brush="#10000000" Geometry="M1,1 H2 V2 H1 Z" />
                                            </DrawingGroup>
                                        </DrawingBrush.Drawing>
                                    </DrawingBrush>
                                </Border.Background>
                            </Border>

                            <ContentPresenter
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
