<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Wpf.Ui.Violeta.Controls">

    <Style x:Key="DefaultSkeletonStyle" TargetType="{x:Type local:Skeleton}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Skeleton}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        ClipToBounds="True"
                        CornerRadius="{DynamicResource ControlCornerRadius}">
                        <Grid>
                            <!--  Actual content  -->
                            <ContentPresenter
                                x:Name="PART_ContentPresenter"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}" />

                            <!--  Solid overlay shown while IsLoading  -->
                            <Border
                                x:Name="PART_LoadingOverlay"
                                IsHitTestVisible="False"
                                Visibility="Collapsed">
                                <Border.Background>
                                    <SolidColorBrush x:Name="OverlayBrush" Color="{DynamicResource SubtleFillColorSecondary}" />
                                </Border.Background>
                            </Border>

                            <!--  Shimmer sweep overlay, only visible when IsLoading + IsActive  -->
                            <Border
                                x:Name="PART_ActiveOverlay"
                                IsHitTestVisible="False"
                                Visibility="Collapsed">
                                <Border.Background>
                                    <LinearGradientBrush x:Name="ShimmerBrush" StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop x:Name="Shimmer0" Offset="-1" Color="Transparent" />
                                        <GradientStop x:Name="Shimmer1" Offset="-0.5" Color="{DynamicResource SubtleFillColorTertiary}" />
                                        <GradientStop x:Name="Shimmer2" Offset="0" Color="Transparent" />
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!--  Show overlay when IsLoading  -->
                        <Trigger Property="IsLoading" Value="True">
                            <Setter TargetName="PART_LoadingOverlay" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <!--  When both IsLoading + IsActive: show shimmer and start animation  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsLoading" Value="True" />
                                <Condition Property="IsActive" Value="True" />
                            </MultiTrigger.Conditions>
                            <MultiTrigger.Setters>
                                <Setter TargetName="PART_ActiveOverlay" Property="Visibility" Value="Visible" />
                            </MultiTrigger.Setters>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard x:Name="ShimmerStoryboard">
                                    <Storyboard
                                        AutoReverse="True"
                                        RepeatBehavior="Forever"
                                        Duration="0:0:1.4">
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Shimmer0" Storyboard.TargetProperty="Offset">
                                            <EasingDoubleKeyFrame KeyTime="0%" Value="-1" />
                                            <EasingDoubleKeyFrame KeyTime="100%" Value="0.5">
                                                <EasingDoubleKeyFrame.EasingFunction>
                                                    <CubicEase EasingMode="EaseInOut" />
                                                </EasingDoubleKeyFrame.EasingFunction>
                                            </EasingDoubleKeyFrame>
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Shimmer1" Storyboard.TargetProperty="Offset">
                                            <EasingDoubleKeyFrame KeyTime="0%" Value="-0.5" />
                                            <EasingDoubleKeyFrame KeyTime="100%" Value="1.0">
                                                <EasingDoubleKeyFrame.EasingFunction>
                                                    <CubicEase EasingMode="EaseInOut" />
                                                </EasingDoubleKeyFrame.EasingFunction>
                                            </EasingDoubleKeyFrame>
                                        </DoubleAnimationUsingKeyFrames>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Shimmer2" Storyboard.TargetProperty="Offset">
                                            <EasingDoubleKeyFrame KeyTime="0%" Value="0" />
                                            <EasingDoubleKeyFrame KeyTime="100%" Value="1.5">
                                                <EasingDoubleKeyFrame.EasingFunction>
                                                    <CubicEase EasingMode="EaseInOut" />
                                                </EasingDoubleKeyFrame.EasingFunction>
                                            </EasingDoubleKeyFrame>
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="ShimmerStoryboard" />
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultSkeletonStyle}" TargetType="{x:Type local:Skeleton}" />

</ResourceDictionary>
