<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Wpf.Ui.Violeta.Controls">

    <!-- Flat progress bar used as the StepBar connector line -->
    <Style x:Key="StepBarProgressBarStyle" TargetType="{x:Type ProgressBar}">
        <Setter Property="Height" Value="2" />
        <Setter Property="Background" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentFillColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ProgressBar}">
                    <Grid x:Name="TemplateRoot" ClipToBounds="True" SnapsToDevicePixels="True">
                        <Rectangle
                            x:Name="PART_Track"
                            Fill="{TemplateBinding Background}"
                            StrokeThickness="0" />
                        <Decorator x:Name="PART_Indicator" HorizontalAlignment="Left">
                            <Rectangle Fill="{TemplateBinding Foreground}" />
                        </Decorator>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter TargetName="PART_Indicator" Property="HorizontalAlignment" Value="Stretch" />
                            <Setter TargetName="PART_Indicator" Property="VerticalAlignment" Value="Bottom" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ItemsPanelTemplate x:Key="StepBarHorizontalItemsPanelTemplate">
        <UniformGrid Rows="1" />
    </ItemsPanelTemplate>

    <ItemsPanelTemplate x:Key="StepBarVerticalItemsPanelTemplate">
        <UniformGrid Columns="1" />
    </ItemsPanelTemplate>

    <!-- StepBarItem base: status-driven foreground -->
    <Style x:Key="DefaultStepBarItemStyle" TargetType="{x:Type local:StepBarItem}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Style.Triggers>
            <Trigger Property="Status" Value="Complete">
                <Setter Property="Foreground" Value="{DynamicResource AccentTextFillColorSecondaryBrush}" />
            </Trigger>
            <Trigger Property="Status" Value="UnderWay">
                <Setter Property="Foreground" Value="{DynamicResource AccentTextFillColorPrimaryBrush}" />
            </Trigger>
            <Trigger Property="Status" Value="Waiting">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="StepBarItemHorizontalTopStyle"
        BasedOn="{StaticResource DefaultStepBarItemStyle}"
        TargetType="{x:Type local:StepBarItem}">
        <Setter Property="Margin" Value="10,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:StepBarItem}">
                    <Border Background="Transparent">
                        <StackPanel>
                            <Border
                                MinWidth="24"
                                Height="24"
                                HorizontalAlignment="Center"
                                Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
                                BorderBrush="{TemplateBinding Foreground}"
                                BorderThickness="2"
                                CornerRadius="12">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{TemplateBinding Foreground}"
                                    Text="{Binding Index, RelativeSource={RelativeSource TemplatedParent}}" />
                            </Border>
                            <ContentPresenter
                                Margin="0,6,0,0"
                                HorizontalAlignment="Center"
                                ContentTemplate="{Binding ItemTemplate, RelativeSource={RelativeSource AncestorType=local:StepBar}}"
                                TextElement.Foreground="{TemplateBinding Foreground}" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="StepBarItemHorizontalBottomStyle"
        BasedOn="{StaticResource DefaultStepBarItemStyle}"
        TargetType="{x:Type local:StepBarItem}">
        <Setter Property="Margin" Value="10,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:StepBarItem}">
                    <Border Background="Transparent">
                        <StackPanel>
                            <ContentPresenter
                                Margin="0,0,0,6"
                                HorizontalAlignment="Center"
                                ContentTemplate="{Binding ItemTemplate, RelativeSource={RelativeSource AncestorType=local:StepBar}}"
                                TextElement.Foreground="{TemplateBinding Foreground}" />
                            <Border
                                MinWidth="24"
                                Height="24"
                                HorizontalAlignment="Center"
                                Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
                                BorderBrush="{TemplateBinding Foreground}"
                                BorderThickness="2"
                                CornerRadius="12">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{TemplateBinding Foreground}"
                                    Text="{Binding Index, RelativeSource={RelativeSource TemplatedParent}}" />
                            </Border>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="StepBarItemVerticalStyle"
        BasedOn="{StaticResource DefaultStepBarItemStyle}"
        TargetType="{x:Type local:StepBarItem}">
        <Setter Property="Margin" Value="0,10" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:StepBarItem}">
                    <Border Background="Transparent">
                        <StackPanel Orientation="Horizontal">
                            <Border
                                MinWidth="24"
                                Height="24"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Top"
                                Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
                                BorderBrush="{TemplateBinding Foreground}"
                                BorderThickness="2"
                                CornerRadius="12">
                                <TextBlock
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{TemplateBinding Foreground}"
                                    Text="{Binding Index, RelativeSource={RelativeSource TemplatedParent}}" />
                            </Border>
                            <ContentPresenter
                                Margin="10,2,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                ContentTemplate="{Binding ItemTemplate, RelativeSource={RelativeSource AncestorType=local:StepBar}}"
                                TextElement.Foreground="{TemplateBinding Foreground}" />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="StepBarHorizontalTopControlTemplate" TargetType="{x:Type local:StepBar}">
        <Grid>
            <ProgressBar
                x:Name="PART_ProgressBarBack"
                Margin="0,11,0,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Top"
                Style="{StaticResource StepBarProgressBarStyle}" />
            <ItemsPresenter />
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="StepBarHorizontalBottomControlTemplate" TargetType="{x:Type local:StepBar}">
        <Grid>
            <ProgressBar
                x:Name="PART_ProgressBarBack"
                Margin="0,0,0,11"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Style="{StaticResource StepBarProgressBarStyle}" />
            <ItemsPresenter VerticalAlignment="Bottom" />
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="StepBarVerticalControlTemplate" TargetType="{x:Type local:StepBar}">
        <Grid>
            <ProgressBar
                x:Name="PART_ProgressBarBack"
                Width="2"
                Margin="11,0,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Orientation="Vertical"
                RenderTransformOrigin="0.5,0.5"
                Style="{StaticResource StepBarProgressBarStyle}">
                <ProgressBar.RenderTransform>
                    <RotateTransform Angle="180" />
                </ProgressBar.RenderTransform>
            </ProgressBar>
            <ItemsPresenter />
        </Grid>
    </ControlTemplate>

    <Style x:Key="DefaultStepBarStyle" TargetType="{x:Type local:StepBar}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource StepBarItemHorizontalTopStyle}" />
        <Setter Property="Template" Value="{StaticResource StepBarHorizontalTopControlTemplate}" />
        <Setter Property="ItemsPanel" Value="{StaticResource StepBarHorizontalItemsPanelTemplate}" />
        <Style.Triggers>
            <Trigger Property="Dock" Value="Left">
                <Setter Property="ItemContainerStyle" Value="{StaticResource StepBarItemVerticalStyle}" />
                <Setter Property="Template" Value="{StaticResource StepBarVerticalControlTemplate}" />
                <Setter Property="ItemsPanel" Value="{StaticResource StepBarVerticalItemsPanelTemplate}" />
            </Trigger>
            <Trigger Property="Dock" Value="Bottom">
                <Setter Property="ItemContainerStyle" Value="{StaticResource StepBarItemHorizontalBottomStyle}" />
                <Setter Property="Template" Value="{StaticResource StepBarHorizontalBottomControlTemplate}" />
                <Setter Property="ItemsPanel" Value="{StaticResource StepBarHorizontalItemsPanelTemplate}" />
            </Trigger>
            <Trigger Property="Dock" Value="Right">
                <Setter Property="FlowDirection" Value="RightToLeft" />
                <Setter Property="ItemContainerStyle" Value="{StaticResource StepBarItemVerticalStyle}" />
                <Setter Property="Template" Value="{StaticResource StepBarVerticalControlTemplate}" />
                <Setter Property="ItemsPanel" Value="{StaticResource StepBarVerticalItemsPanelTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource DefaultStepBarStyle}" TargetType="{x:Type local:StepBar}" />

    <Style BasedOn="{StaticResource DefaultStepBarItemStyle}" TargetType="{x:Type local:StepBarItem}" />

</ResourceDictionary>
