<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Wpf.Ui.Violeta.Controls"
    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <system:Double x:Key="NumericUpDownSpinButtonWidth">22</system:Double>
    <system:Double x:Key="NumericUpDownSpinButtonHeight">14</system:Double>

    <!--  Internal spin-button style (shared by IncreaseButton and DecreaseButton)  -->
    <Style x:Key="NumericUpDownSpinButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}" />
        <Setter Property="Width" Value="{StaticResource NumericUpDownSpinButtonWidth}" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorDisabledBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  Base NumericUpDown style.
          All concrete typed subclasses inherit this via BasedOn.  -->
    <Style x:Key="BaseNumericUpDownStyle" TargetType="{x:Type local:NumericUpDown}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource DefaultControlFocusVisualStyle}" />
        <Setter Property="ContextMenu" Value="{DynamicResource DefaultControlContextMenu}" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="Background" Value="{DynamicResource TextControlBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TextControlElevationBorderBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource TextBoxBorderThemeThickness}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="Padding" Value="{DynamicResource TextControlThemePadding}" />
        <Setter Property="MinHeight" Value="{DynamicResource TextControlThemeMinHeight}" />
        <Setter Property="MinWidth" Value="{DynamicResource TextControlThemeMinWidth}" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Border.CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NumericUpDown}">
                    <Grid>
                        <!--  Main container border  -->
                        <Border
                            x:Name="ContentBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}"
                            SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <!--  InnerLeftContent  -->
                                    <ColumnDefinition Width="*" />
                                    <!--  TextBox / DragPanel  -->
                                    <ColumnDefinition Width="Auto" />
                                    <!--  InnerRightContent  -->
                                    <ColumnDefinition Width="Auto" />
                                    <!--  SpinnerPanel  -->
                                </Grid.ColumnDefinitions>

                                <!--  InnerLeftContent  -->
                                <ContentPresenter
                                    x:Name="PART_InnerLeftContent"
                                    Grid.Column="0"
                                    Margin="8,0,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding InnerLeftContent}"
                                    Visibility="Collapsed" />

                                <!--  Text area: stacks TextBox, placeholder and drag-panel overlay  -->
                                <Grid Grid.Column="1">
                                    <!--  Editable TextBox  -->
                                    <TextBox
                                        x:Name="PART_TextBox"
                                        Padding="{TemplateBinding Padding}"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="Stretch"
                                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                        AcceptsReturn="False"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        BorderThickness="0"
                                        Style="{DynamicResource TransparentTextBoxStyle}"
                                        CaretBrush="{DynamicResource TextFillColorPrimaryBrush}"
                                        FontFamily="{TemplateBinding FontFamily}"
                                        FontSize="{TemplateBinding FontSize}"
                                        FontWeight="{TemplateBinding FontWeight}"
                                        Foreground="{TemplateBinding Foreground}"
                                        ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                        ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                        SelectionBrush="{DynamicResource TextControlSelectionHighlightColor}"
                                        TextWrapping="NoWrap" />

                                    <!--  Placeholder text (shown when TextBox is empty)  -->
                                    <TextBlock
                                        x:Name="PART_PlaceholderText"
                                        Margin="{TemplateBinding Padding}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        FontFamily="{TemplateBinding FontFamily}"
                                        FontSize="{TemplateBinding FontSize}"
                                        Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                        IsHitTestVisible="False"
                                        Text="{TemplateBinding PlaceholderText}"
                                        TextWrapping="NoWrap"
                                        Visibility="Collapsed" />

                                    <!--  Drag-panel overlay (AllowDrag mode)  -->
                                    <Border
                                        x:Name="PART_DragPanel"
                                        Background="Transparent"
                                        Cursor="SizeAll"
                                        Visibility="Collapsed" />
                                </Grid>

                                <!--  InnerRightContent  -->
                                <ContentPresenter
                                    x:Name="PART_InnerRightContent"
                                    Grid.Column="2"
                                    Margin="0,0,8,0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding InnerRightContent}"
                                    Visibility="Collapsed" />

                                <!--  SpinnerPanel: vertical separator + up/down repeat buttons  -->
                                <Grid
                                    x:Name="PART_SpinnerPanel"
                                    Grid.Column="3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <!--  separator  -->
                                        <ColumnDefinition Width="Auto" />
                                        <!--  buttons  -->
                                    </Grid.ColumnDefinitions>

                                    <!--  Vertical separator between text area and buttons  -->
                                    <Border
                                        x:Name="PART_SpinnerSeparator"
                                        Grid.Column="0"
                                        Width="1"
                                        Margin="0,6"
                                        Background="{DynamicResource ControlStrokeColorDefaultBrush}" />

                                    <!--  Up / Down buttons  -->
                                    <StackPanel Grid.Column="1" Orientation="Vertical">
                                        <RepeatButton
                                            x:Name="PART_IncreaseButton"
                                            Height="{StaticResource NumericUpDownSpinButtonHeight}"
                                            Style="{StaticResource NumericUpDownSpinButtonStyle}">
                                            <TextBlock
                                                FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                FontSize="9"
                                                Text="&#xE70E;" />
                                        </RepeatButton>
                                        <Border
                                            Height="1"
                                            Margin="3,0"
                                            Background="{DynamicResource ControlStrokeColorDefaultBrush}" />
                                        <RepeatButton
                                            x:Name="PART_DecreaseButton"
                                            Height="{StaticResource NumericUpDownSpinButtonHeight}"
                                            Style="{StaticResource NumericUpDownSpinButtonStyle}">
                                            <TextBlock
                                                FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                                FontSize="9"
                                                Text="&#xE70D;" />
                                        </RepeatButton>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>

                        <!--  Accent bottom border (highlighted when focused)  -->
                        <Border
                            x:Name="AccentBorder"
                            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                            BorderThickness="{DynamicResource TextBoxAccentBorderThemeThickness}"
                            CornerRadius="{TemplateBinding Border.CornerRadius}"
                            IsHitTestVisible="False" />
                    </Grid>

                    <ControlTemplate.Triggers>

                        <!--  Keyboard focus within: accent highlight  -->
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="AccentBorder" Property="BorderThickness" Value="0,0,0,2" />
                            <Setter TargetName="AccentBorder" Property="BorderBrush" Value="{DynamicResource TextControlFocusedBorderBrush}" />
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource TextControlBackgroundFocused}" />
                        </Trigger>

                        <!--  Pointer hover (unfocused)  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsKeyboardFocusWithin" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource TextControlBackgroundPointerOver}" />
                        </MultiTrigger>

                        <!--  Disabled  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource TextControlBackgroundDisabled}" />
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource TextControlBorderBrushDisabled}" />
                            <Setter TargetName="AccentBorder" Property="BorderBrush" Value="{DynamicResource TextControlBorderBrushDisabled}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextControlForegroundDisabled}" />
                        </Trigger>

                        <!--  ShowButtonSpinner = False: hide buttons column  -->
                        <Trigger Property="ShowButtonSpinner" Value="False">
                            <Setter TargetName="PART_SpinnerPanel" Property="Visibility" Value="Collapsed" />
                        </Trigger>

                        <!--  IsReadOnly: grey-out buttons  -->
                        <Trigger Property="IsReadOnly" Value="True">
                            <Setter TargetName="PART_IncreaseButton" Property="IsEnabled" Value="False" />
                            <Setter TargetName="PART_DecreaseButton" Property="IsEnabled" Value="False" />
                        </Trigger>

                        <!--  Placeholder: visible when TextBox text is empty  -->
                        <DataTrigger Binding="{Binding ElementName=PART_TextBox, Path=Text}" Value="">
                            <Setter TargetName="PART_PlaceholderText" Property="Visibility" Value="Visible" />
                        </DataTrigger>

                        <!--  InnerLeftContent: show when non-null  -->
                        <!--  InnerRightContent: show when non-null  -->
                        <!--  Visibility is controlled by code-behind in OnApplyTemplate  -->

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  One implicit style per concrete type – each BasedOn the base style above.
          This is necessary because WPF's default-style lookup works best when
          DefaultStyleKey points to the element's own type.  -->
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericIntUpDown}" />
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericUIntUpDown}" />
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericDoubleUpDown}" />
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericFloatUpDown}" />
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericDecimalUpDown}" />
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericLongUpDown}" />
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericULongUpDown}" />
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericShortUpDown}" />
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericUShortUpDown}" />
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericByteUpDown}" />
    <Style BasedOn="{StaticResource BaseNumericUpDownStyle}" TargetType="{x:Type local:NumericSByteUpDown}" />

</ResourceDictionary>
