<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Wpf.Ui.Violeta.Controls">

    <!--  BasedOn StaticResource only resolves within this dictionary (and its merges), not sibling MergedDictionaries.  -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/Wpf.Ui.Violeta;component/Controls/SplitButton/SplitButton.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--
        Toolbar SplitButton: transparent chrome by default; SubtleFill hover / press
        (same visual language as ToolBarButtonStyle).
        - Chevron: compact inset (4,0,6,0) via Style.Resources override of
        DefaultSplitButtonToggleButtonStyle (template uses DynamicResource).
        Keeps DefaultSplitButtonStyle template + chevron animation; only retargets
        fill / border brushes + chevron inset.
    -->
    <Style
        x:Key="SimpleSplitButtonStyle"
        BasedOn="{StaticResource DefaultSplitButtonStyle}"
        TargetType="{x:Type local:SplitButton}">
        <Setter Property="Tag" Value="ToolBar" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <!--  Transparent toolbar chrome uses SubtleFill (not ControlFill/ButtonBackground*) so hover is visible on light surfaces.  -->
        <Setter Property="MouseOverBackground" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
        <Setter Property="MouseOverBorderBrush" Value="{DynamicResource ButtonBorderBrushPointerOver}" />
        <!--  WinUI SplitButtonCommandBarStyle: unfocused segment uses SplitButtonInAppBarUnfocusedPointerOver (SubtleFill tertiary).  -->
        <Setter Property="MouseOverSecondaryBackground" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
        <Setter Property="PressedBackground" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
        <Setter Property="PressedSecondaryBackground" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
        <Setter Property="PressedBorderBrush" Value="{DynamicResource ButtonBorderBrushPressed}" />
        <Setter Property="PressedForeground" Value="{DynamicResource ButtonForegroundPressed}" />
    </Style>

    <Style
        x:Key="ToolBarSplitButtonStyle"
        BasedOn="{StaticResource SimpleSplitButtonStyle}"
        TargetType="{x:Type local:SplitButton}">
        <!--
            Same key as DefaultSplitButtonToggleButtonStyle so the template's DynamicResource
            resolves to this compact inset (matches ToolBarToggleComboBoxStyle chevron).
        -->
        <Style.Resources>
            <Style x:Key="DefaultSplitButtonToggleButtonStyle" TargetType="{x:Type ToggleButton}">
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Border.CornerRadius" Value="0" />
                <Setter Property="SnapsToDevicePixels" Value="True" />
                <Setter Property="OverridesDefaultStyle" Value="True" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Border
                                x:Name="ContentBorder"
                                Padding="4,0,6,0"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding Border.CornerRadius}">
                                <ContentPresenter
                                    x:Name="PART_ContentHost"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Style.Resources>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False" />
        </Style.Triggers>
    </Style>

</ResourceDictionary>
