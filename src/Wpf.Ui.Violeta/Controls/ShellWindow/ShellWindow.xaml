<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Violeta.Controls"
    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <system:Double x:Key="MainWindowCaptionHeight">32</system:Double>
    <Thickness x:Key="MainWindowResizeThickness">6</Thickness>

    <Style
        x:Key="DefaultShellWindowStyle"
        BasedOn="{StaticResource {x:Type Window}}"
        TargetType="{x:Type controls:ShellWindow}">
        <Setter Property="Background" Value="{DynamicResource WindowBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource WindowForeground}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="600" />
        <Setter Property="MinHeight" Value="320" />
        <Setter Property="Width" Value="1100" />
        <Setter Property="MinWidth" Value="460" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <!--
            WindowChrome is applied in ShellWindow.SetWindowChrome so GlassFrameThickness
            can switch between -1 (system backdrop) and 1 (solid background).
        -->
    </Style>

    <Style BasedOn="{StaticResource DefaultShellWindowStyle}" TargetType="{x:Type controls:ShellWindow}" />

</ResourceDictionary>
