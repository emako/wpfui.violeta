<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Wpf.Ui.Violeta.Controls"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Style TargetType="{x:Type local:StorageRing}">
        <Setter Property="Width" Value="72" />
        <Setter Property="Height" Value="72" />
        <Setter Property="MinWidth" Value="32" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="ValueRingThickness" Value="6" />
        <Setter Property="TrackRingThickness" Value="6" />
        <Setter Property="PercentCaution" Value="75" />
        <Setter Property="PercentCritical" Value="90" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:StorageRing}">
                    <Border
                        x:Name="PART_Root"
                        Background="Transparent"
                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <Grid>
                            <ui:Arc
                                x:Name="PART_TrackRing"
                                StartAngle="0"
                                EndAngle="359.9"
                                Stretch="None"
                                StrokeStartLineCap="Flat"
                                StrokeEndLineCap="Flat"
                                StrokeThickness="{TemplateBinding TrackRingThickness}" />
                            <ui:Arc
                                x:Name="PART_ValueRing"
                                StartAngle="0"
                                EndAngle="{TemplateBinding ValueAngle}"
                                Stretch="None"
                                StrokeStartLineCap="Flat"
                                StrokeEndLineCap="Flat"
                                StrokeThickness="{TemplateBinding ValueRingThickness}" />
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
