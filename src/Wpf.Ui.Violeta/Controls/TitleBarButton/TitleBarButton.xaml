<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Wpf.Ui.Violeta.Controls"
    xmlns:system="clr-namespace:System;assembly=System.Runtime">

    <system:Double x:Key="TitleBarButtonWidth">46</system:Double>
    <system:Double x:Key="TitleBarButtonIconFontSize">10</system:Double>
    <system:Double x:Key="TitleBarButtonInactiveForegroundOpacity">0.382</system:Double>
    <system:Double x:Key="TitleBarButtonPressedForegroundOpacity">0.618</system:Double>
    <system:Double x:Key="TitleBarButtonDisabledForegroundOpacity">0.382</system:Double>

    <Storyboard x:Key="TitleBarButtonRightToLeftPressStoryboard">
        <DoubleAnimation
            Storyboard.TargetName="IconScaleTransform"
            Storyboard.TargetProperty="ScaleX"
            To="0.66"
            Duration="0:0:0.08" />
    </Storyboard>

    <Storyboard x:Key="TitleBarButtonRightToLeftReleaseStoryboard">
        <DoubleAnimation
            Storyboard.TargetName="IconScaleTransform"
            Storyboard.TargetProperty="ScaleX"
            To="1.0"
            Duration="0:0:0.08" />
    </Storyboard>

    <Style x:Key="TitleBarButtonDefaultStyle" TargetType="{x:Type local:TitleBarButton}">
        <Setter Property="Width" Value="{StaticResource TitleBarButtonWidth}" />
        <Setter Property="Background" Value="{DynamicResource TitleBarButtonBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource TitleBarButtonForeground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TitleBarButton}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Border.Resources>
                            <Style TargetType="{x:Type Control}">
                                <Setter Property="Focusable" Value="False" />
                            </Style>
                        </Border.Resources>
                        <ContentPresenter
                            x:Name="PART_ContentPresenter"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                            RenderTransformOrigin="0.5,0.5">
                            <ContentPresenter.RenderTransform>
                                <ScaleTransform x:Name="IconScaleTransform" ScaleX="1.0" ScaleY="1.0" />
                            </ContentPresenter.RenderTransform>
                        </ContentPresenter>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsActive" Value="False">
                            <Setter TargetName="PART_ContentPresenter" Property="Opacity" Value="{StaticResource TitleBarButtonInactiveForegroundOpacity}" />
                        </Trigger>
                        <Trigger Property="PressAnimation" Value="RightToLeft">
                            <Setter TargetName="PART_ContentPresenter" Property="RenderTransformOrigin" Value="1,0.5" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="PressAnimation" Value="Center" />
                                <Condition Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource TitleBarButtonRightToLeftPressStoryboard}" />
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource TitleBarButtonRightToLeftReleaseStoryboard}" />
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="PressAnimation" Value="RightToLeft" />
                                <Condition Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource TitleBarButtonRightToLeftPressStoryboard}" />
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource TitleBarButtonRightToLeftReleaseStoryboard}" />
                            </MultiTrigger.ExitActions>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource TitleBarButtonDisabledBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource TitleBarButtonDisabledForeground}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource TitleBarButtonHoverBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource TitleBarButtonHoverForeground}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource TitleBarButtonPressedBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource TitleBarButtonPressedForeground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource TitleBarButtonDefaultStyle}" TargetType="{x:Type local:TitleBarButton}" />

</ResourceDictionary>