namespace Wpf.Ui.Violeta.Controls;

/// <summary>
/// Defines where the label is placed relative to the content in a <see cref="DescriptionsItem"/>.
/// Only <see cref="Top"/> and <see cref="Left"/> are supported in templates.
/// </summary>
public enum DescriptionsLabelPosition
{
    Left,
    Top,
    Right,
    Bottom,
}
