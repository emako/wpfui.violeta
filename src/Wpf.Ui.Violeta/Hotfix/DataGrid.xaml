<!--
    Hotfix: DataGridRow selection pill ScaleY animation (same as ListViewItem / UI.WPF.Modern).
    Overrides DefaultDataGridRowStyle; also redefines DefaultDataGridStyle / DefaultUiDataGridStyle
    because RowStyle is StaticResource in WPF-UI.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:compat="clr-namespace:Wpf.Ui.Violeta.Controls.Compat"
    xmlns:controls="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Style x:Key="DefaultDataGridRowStyle" TargetType="{x:Type DataGridRow}">
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Validation.ErrorTemplate" Value="{x:Null}" />
        <Setter Property="ValidationErrorTemplate">
            <Setter.Value>
                <ControlTemplate>
                    <controls:SymbolIcon
                        Width="24"
                        Height="24"
                        Margin="-4,0,-12,0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                        Symbol="ErrorCircle12" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridRow}">
                    <Border
                        x:Name="DGR_Border"
                        compat:PressHelper.IsEnabled="True"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource ControlCornerRadius}"
                        SnapsToDevicePixels="True">
                        <SelectiveScrollingGrid>
                            <SelectiveScrollingGrid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </SelectiveScrollingGrid.ColumnDefinitions>
                            <SelectiveScrollingGrid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </SelectiveScrollingGrid.RowDefinitions>
                            <DataGridCellsPresenter
                                Grid.Column="1"
                                ItemsPanel="{TemplateBinding ItemsPanel}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <DataGridDetailsPresenter
                                Grid.Row="1"
                                Grid.Column="1"
                                SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding AreRowDetailsFrozen, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, Converter={x:Static DataGrid.RowDetailsScrollingConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                Visibility="{TemplateBinding DetailsVisibility}" />
                            <DataGridRowHeader
                                Grid.Row="0"
                                Grid.RowSpan="2"
                                Grid.Column="0"
                                SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                                Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Row}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                            <Rectangle
                                x:Name="ActiveRectangle"
                                Grid.Row="0"
                                Grid.Column="0"
                                Width="3"
                                Height="18"
                                Margin="0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Fill="{DynamicResource ListViewItemPillFillBrush}"
                                Opacity="0"
                                RadiusX="2"
                                RadiusY="2"
                                RenderTransformOrigin="0.5,0.5">
                                <Rectangle.RenderTransform>
                                    <ScaleTransform x:Name="PillTransform" ScaleY="1" />
                                </Rectangle.RenderTransform>
                            </Rectangle>
                        </SelectiveScrollingGrid>
                        <!--
                            DataGridRow drives CommonStates (Normal / MouseOver / *_Selected / …).
                            Pill press/select animations use Trigger EnterActions instead of VSM
                            so they do not fight those states.
                        -->
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="Normal_AlternatingRow" />
                                <VisualState x:Name="Normal_Selected" />
                                <VisualState x:Name="Unfocused_Selected" />
                                <VisualState x:Name="Normal_Editing" />
                                <VisualState x:Name="MouseOver_Editing" />
                                <VisualState x:Name="MouseOver_Unfocused_Editing" />
                                <VisualState x:Name="Unfocused_Editing" />
                                <VisualState x:Name="MouseOver" />
                                <VisualState x:Name="MouseOver_Selected" />
                                <VisualState x:Name="MouseOver_Unfocused_Selected" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="AlternationIndex" Value="0">
                            <Setter TargetName="DGR_Border" Property="Background" Value="{Binding RowBackground, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                        </Trigger>
                        <Trigger Property="AlternationIndex" Value="1">
                            <Setter TargetName="DGR_Border" Property="Background" Value="{Binding AlternatingRowBackground, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="DGR_Border" Property="Background" Value="{DynamicResource ListViewItemBackgroundPointerOver}" />
                        </MultiTrigger>
                        <!--
                            Trigger action Storyboards are frozen when the template seals.
                            DynamicResource cannot freeze; StaticResource cannot see Variables.xaml
                            from this merged dictionary — use literals matching Variables.xaml.
                        -->
                        <Trigger SourceName="DGR_Border" Property="compat:PressHelper.IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PillTransform" Storyboard.TargetProperty="ScaleY">
                                            <SplineDoubleKeyFrame KeySpline="0,0,0,1" KeyTime="0:0:0.167" Value="0.625" />
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PillTransform" Storyboard.TargetProperty="ScaleY">
                                            <SplineDoubleKeyFrame KeySpline="1,0,0,0" KeyTime="0:0:0.167" Value="1" />
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ActiveRectangle" Property="Opacity" Value="1" />
                            <Setter TargetName="DGR_Border" Property="Background" Value="{DynamicResource ListViewItemBackgroundPointerOver}" />
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimationUsingKeyFrames Storyboard.TargetName="PillTransform" Storyboard.TargetProperty="ScaleY">
                                            <DiscreteDoubleKeyFrame KeyTime="0" Value="0.625" />
                                            <SplineDoubleKeyFrame KeySpline="0,0,0,1" KeyTime="0:0:0.167" Value="1" />
                                        </DoubleAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        RowStyle on WPF-UI DefaultDataGridStyle is StaticResource — re-bind to our row style.
        BasedOn resolves to the previously merged WPF-UI DefaultDataGridStyle (same key not yet in this dict).
    -->
    <Style
        x:Key="DefaultDataGridStyle"
        BasedOn="{StaticResource DefaultDataGridStyle}"
        TargetType="{x:Type DataGrid}">
        <Setter Property="RowStyle" Value="{StaticResource DefaultDataGridRowStyle}" />
    </Style>

    <Style
        x:Key="DefaultUiDataGridStyle"
        BasedOn="{StaticResource DefaultDataGridStyle}"
        TargetType="{x:Type controls:DataGrid}">
        <Setter Property="CellStyle" Value="{StaticResource DefaultUiDataGridCellStyle}" />
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource DefaultUiDataGridColumnHeaderStyle}" />
        <Setter Property="CheckBoxColumnEditingElementStyle" Value="{StaticResource DefaultDataGridCheckBoxEditingElementStyle}" />
        <Setter Property="CheckBoxColumnElementStyle" Value="{StaticResource DefaultDataGridCheckBoxElementStyle}" />
        <Setter Property="ComboBoxColumnEditingElementStyle" Value="{StaticResource DefaultDataGridComboBoxEditingElementStyle}" />
        <Setter Property="ComboBoxColumnElementStyle" Value="{StaticResource DefaultDataGridComboBoxElementStyle}" />
        <Setter Property="TextColumnEditingElementStyle" Value="{StaticResource DefaultDataGridTextEditingElementStyle}" />
        <Setter Property="TextColumnElementStyle" Value="{StaticResource DefaultDataGridTextElementStyle}" />
    </Style>

    <Style BasedOn="{StaticResource DefaultDataGridStyle}" TargetType="{x:Type DataGrid}" />
    <Style BasedOn="{StaticResource DefaultUiDataGridStyle}" TargetType="{x:Type controls:DataGrid}" />

</ResourceDictionary>
