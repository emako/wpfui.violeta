<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui">

    <!--
        Flyout ContextMenu for DropDownButton / SplitButton (Placement=Bottom).
        Same chrome as UiContextMenu, but without the From=-90 slide-in that
        briefly covers the host button. Right-click ContextMenus stay untouched.
    -->
    <Style
        x:Key="DropDownFlyoutContextMenuStyle"
        BasedOn="{StaticResource UiContextMenu}"
        TargetType="{x:Type ContextMenu}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <controls:EffectThicknessDecorator Thickness="30">
                        <Border
                            x:Name="Border"
                            Padding="0,3,0,3"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <Border.Effect>
                                <DropShadowEffect
                                    BlurRadius="20"
                                    Direction="270"
                                    Opacity="0.135"
                                    ShadowDepth="10"
                                    Color="#202020" />
                            </Border.Effect>
                            <StackPanel
                                ClipToBounds="True"
                                IsItemsHost="True"
                                KeyboardNavigation.DirectionalNavigation="Cycle"
                                Orientation="Vertical" />
                        </Border>
                    </controls:EffectThicknessDecorator>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
